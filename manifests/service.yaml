apiVersion: v1
kind: Service
metadata:
  name: sample-spring-boot-app-openshift2
  labels:
    app: sample-spring-boot-app-openshift2
  annotations:
    backstage.io/kubernetes-id: sample-spring-boot-app-openshift2
spec:
  type: ClusterIP
  selector:
    app: sample-spring-boot-app-openshift2
  ports:
    - port: 8080
      protocol: TCP
      name: http